import { PictureOrPlaceholderPipe } from './picture-or-placeholder.pipe';

describe('PictureOrPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new PictureOrPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
